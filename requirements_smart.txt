# 核心
pyautogui>=0.9.54
mss>=9.0.1
opencv-python>=4.8.0
numpy>=1.24.0
rapidfuzz>=3.9.1

# OCR 方案二选一（代码会自动优先 easyocr，失败回退到 Tesseract）
# 1) EasyOCR 路线（需 torch，Python3.13 上可能需要额外 wheel）
easyocr>=1.7.1; platform_system != "Windows" or python_version < "3.13"

# 2) Tesseract 路线（推荐在 Windows + Python3.13）
pytesseract>=0.3.13
Pillow>=10.0.0